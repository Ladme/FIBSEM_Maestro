[build-system]
requires = ["setuptools>=64", "setuptools-scm>=8"]
build-backend = "setuptools.build_meta"

[project]
name = "fibsem-maestro"
description = "Software for (cryo) volume-EM acquisition."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "colorama>=0.4.6",
    "matplotlib>=3.8.1",
    "numpy==2.2.5",
    "opencv-python-headless>=4.13.0.92",
    "pillow>=12.0.0",
    "pydantic==2.12.4",
    "pyyaml==6.0.3",
    "scipy==1.15.3",
    "tifffile==2025.3.13",
    "pyqt6>=6.4.2",
    "pyqt6-qt6<6.5",
    "qdarkstyle>=3.2.3",
    "platformdirs>=4.10.0",
]
dynamic = ["version"]

[dependency-groups]
dev = [
    "codecov>=2.1.13",
    "pytest>=9.0.1",
    "pytest-cov>=7.1.0",
    "ruff>=0.14.6",
    "ty>=0.0.34",
]

[tool.setuptools_scm]
write_to = "src/fibsem_maestro/_version.py"

[tool.ruff]
exclude = ["src/fibsem_maestro_original", "src/criterion_playground.py"]

[tool.ruff.lint]
extend-select = [
    "F",   # Pyflakes rules
    "W",   # PyCodeStyle warnings
    "E",   # PyCodeStyle errors
    "I",   # Sort imports properly
    "UP",  # Warn if certain things can changed due to newer Python versions
    "C4",  # Catch incorrect use of comprehensions, dict, list, etc
    "FA",  # Enforce from __future__ import annotations
    "ISC", # Good use of string concatenation
    "ICN", # Use common import conventions
    "RET", # Good return practices
    "SIM", # Common simplification rules
    "TID", # Some good import practices
    "TC",  # Enforce importing certain types in a TYPE_CHECKING block
    "PTH", # Use pathlib instead of os.path
    "NPY", # Some numpy-specific things
    "ARG", # Catch incorrect use of arguments
]
ignore = ["E501"]

[tool.ty.src]
exclude = ["src/fibsem_maestro_original", "src/criterion_playground.py"]

[tool.uv]
package = true

[project.gui-scripts]
fibsem-maestro = "fibsem_maestro.main:main"
