[project]
name = "fibsem-maestro"
version = "0.1.0"
description = "Software for (cryo) volume-EM acquisition."
readme = "README.md"
requires-python = "==3.11.2"
dependencies = [
    "pydantic>=2.12.4",
]
